---
import "../styles/global.css";
import Sidebar from "@components/astro/Sidebar.astro";

import ThemeScript from "@components/astro/ThemeScript.astro";
import TableOfContents from "@components/astro/TableOfContents.astro";
import ThemeTabs from "@components/svelte/ThemeTabs.svelte";
import { ViewTransitions } from "astro:transitions";

interface Frontmatter {
  title: string;
}

interface Props {
  frontmatter?: Frontmatter;
  title?: string;
  headings: { depth: number; slug: string; text: string }[];
}

const { frontmatter, headings } = Astro.props;
const title = frontmatter?.title ?? Astro.props.title ?? "Docs";
---

<!doctype html>
<html lang="es" class="scroll-smooth">
  <head>
    <ThemeScript />
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>{title} | Shizen UI</title>
    <ViewTransitions />
  </head>
  <body class="bg-background font-sans">
    <div class="mx-auto max-w-8xl px-6">
      <div class="flex flex-col md:flex-row gap-8">
        <div transition:persist="sidebar">
          <Sidebar />
        </div>

        <main
          class="flex-auto py-10 md:pl-72 lg:pr-72 min-w-0 flex justify-center"
          transition:animate="fade"
        >
          <article class="prose-content w-full max-w-160">
            <slot />
          </article>
        </main>

        <div
          transition:persist="toc"
          class="hidden lg:flex flex-col items-end gap-3 fixed top-8 right-[max(0px,calc(50%-45rem))] w-64 pr-14 z-30"
        >
          <ThemeTabs client:load />
          <TableOfContents headings={headings} />
        </div>
      </div>
    </div>
  </body>
</html>
